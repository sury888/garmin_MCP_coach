[project]
name = "garmincustommcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Suryansh Yadav", email = "suryyadav@example.com" }
]
requires-python = ">=3.12"
dependencies = [
    "garminconnect>=0.3.11",
    "mcp[cli]>=2.1.1",
    "python-dotenv>=1.2.3",
    "sqlalchemy>=2.0.52",
]

[project.scripts]
garmincustommcp = "garmincustommcp:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
